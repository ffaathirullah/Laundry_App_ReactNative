import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const Pesanan = ({}) => (
  <View style={styles.container}>
    <Text>I'm Pesanan</Text>
  </View>
);

export default Pesanan;

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
