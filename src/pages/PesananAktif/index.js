import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const PesananAktif = ({}) => (
  <View style={styles.container}>
    <Text>I'm PesananAktif</Text>
  </View>
);

export default PesananAktif;

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
